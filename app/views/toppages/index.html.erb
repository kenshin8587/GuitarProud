<% if logged_in? %>
  <%= form_tag(root_url, method: 'get') do %>
    <div class="row mb-1">
      <div class="col-md-6">
        <%= text_field_tag :bandname, params[:bandname], placeholder: 'バンド名', class: 'form-control form-control-sm' %>
      </div>
      <div class="offset-md-6"></div>
    </div>
    
    <div class="row">
      <div class="col-md-6">
        <%= text_field_tag :part, params[:part], placeholder: '足りないパート', class: 'form-control form-control-sm' %>
      </div>
      <div class="offset-md-6"></div>
    </div>
      
    <%= submit_tag '検索', class: 'btn btn-warning btn-sm mt-1 mb-2' %>
  <% end %>
  <div class="row">
    <%= render 'posts/post', posts: @posts %>
  </div>
<% else %>
  <div class="card bg-dark text-white mb-3">
    <%= image_tag '/Users/takenobuseo/Documents/ruby/first_ruby/app/assets/images/LUNA_SEA.jpeg', :size =>'320x480', class: 'card-img-top' %>
    <div class="card-body">
      <h5 class="card-title">Welcome to GuitarProud!</h5>
      <p lass="card-text"><%= link_to 'アカウントを作成', signup_path %></p>
    </div>
  </div>
<% end %>
